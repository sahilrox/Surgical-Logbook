<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.surgicallogbook">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">

        </activity>
        <activity android:name=".HomePage" />
        <activity android:name=".Operation" android:theme="@style/AppTheme"/>
        <activity android:name=".ImagesActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".LoginPage" android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Media" android:theme="@style/AppTheme"/>
        <activity android:name=".PasswordActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".ProfileActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".SignUpPage" android:theme="@style/AppTheme"/>
        <activity android:name=".SurgicalProcedure" android:theme="@style/AppTheme"/>
    </application>

</manifest>